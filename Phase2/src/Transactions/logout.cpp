#include "../include/Transactions/logout.h"

void Logout::processTransaction() {
    // TODO end a Front End session
    // TODO should write out the daily transaction file (see below) and stop accepting any transactions except login
    // TODO should only be accepted when logged in
    // TODO no transaction other than login should be accepted after a logout
}